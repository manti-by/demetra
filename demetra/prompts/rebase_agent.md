The following files have rebase conflicts that must be resolved:

{conflicted_files}

Rebase command error output:
{rebase_error}

Resolve all conflicts. When a conflict occurs, prefer the version from the branch being rebased onto (theirs/base branch) over the current branch's version. After resolving each conflict, stage the file with `git add`. Do NOT run `git rebase --continue`.
