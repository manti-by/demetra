The following files have merge conflicts that must be resolved:

{conflicted_files}

Merge command error output:
{merge_error}

Resolve all conflicts. When a conflict occurs, prefer the version from the branch being merged in (theirs/base branch) over the current branch's version. After resolving each conflict, stage the file with `git add`. Do NOT commit.
