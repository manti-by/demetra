## PR creation failed

The build, commit, and push steps succeeded, but creating the
GitHub pull request failed. The branch has been pushed; the PR has
not been created.

**Branch:** `{branch_name}`
**Open manually:** https://github.com/{repository_owner}/{repository_name}/compare/{branch_name}

### Error

```
{error}
```

Please create the PR manually, then move the ticket back to `In Review` to continue.
